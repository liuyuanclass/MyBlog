<p id="notice"><%= notice %></p>

<p>
  <strong>院系:</strong>
  <%= link_to @department.depart_id,class1s_path %>
</p>

<p>
  <strong>简介:</strong>
  <%= @department.describe %>
</p>
<table>
  <thead>
  <tr>
    <th>班级名称</th>
    <th>所述系</th>
    <th>班主任ID</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @class1s.each do |class1| %>
    <tr>
      <td><%= class1.class_id %></td>
      <td><%= class1.depart_id %></td>
      <td><%= class1.director_id %></td>
      <td><%= link_to '详情', class1 %></td>
      <td><%= link_to '修改', edit_class1_path(class1) %></td>
      <td><%= link_to '删除', class1, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= link_to '编辑', edit_department_path(@department) %> |
<%= link_to '返回', departments_path %>
