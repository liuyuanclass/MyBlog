<h1>我的账户</h1>

<table  class="table table-hover">
  <thead>
  <tr>
    <th>账户</th>
    <th>姓名</th>
    <th>余额</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>

    <tr>
      <td><%= @account.account_id %></td>
      <td><%= @student.name %></td>
      <td><%= @account.balance%></td>
    </tr>

  </tbody>
   <td><%=render "charge",account: @account%></td>
</table>

<%= link_to "交易记录",read_records_path(current_user) %>
<%= link_to "缴纳电费",read_energy_path(current_user) %>
<%= link_to "网费缴纳",read_internet_path(current_user) %>
