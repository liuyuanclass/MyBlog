<%= form_with(model: organize, local: true) do |form| %>
  <% if organize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organize.errors.count, "error") %> prohibited this organize from being saved:</h2>

      <ul>
        <% organize.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :community_id %>
    <%= form.text_field :community_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
